"""Validation pipeline scaffold.

Implementation intentionally deferred while the assignment structure and
documentation are being prepared.
"""

from pathlib import Path


def run_validation(input_path: Path, output_dir: Path) -> None:
    """Placeholder entry point for the validation pipeline."""
    raise NotImplementedError(
        f"Validation pipeline not implemented yet. Planned input={input_path} output={output_dir}"
    )
