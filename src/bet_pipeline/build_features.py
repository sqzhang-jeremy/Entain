"""Feature generation scaffold.

Implementation intentionally deferred while the assignment structure and
documentation are being prepared.
"""

from pathlib import Path


def run_feature_build(input_path: Path, output_dir: Path) -> None:
    """Placeholder entry point for customer feature generation."""
    raise NotImplementedError(
        f"Feature pipeline not implemented yet. Planned input={input_path} output={output_dir}"
    )
