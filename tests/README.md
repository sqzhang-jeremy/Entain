# Tests Placeholder

This directory is reserved for the final test suite.

Planned coverage areas:

- rule-level validation checks for domain and numeric constraints
- payout and `return_for_entain` derivation checks
- duplicate and ordering consistency checks
- deterministic handling of invalid rows within a customer's first 20 bets
- customer aggregation correctness
- CLI smoke tests

Replace this file with actual tests during implementation.
